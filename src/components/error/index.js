import * as React from 'react';


export const ErrorHandler = () =>{
    return (
        <div role="alert">
        <p>An error occurred:</p>
        <pre> Sorry we are working on it  </pre>
      </div>
    );
}